services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_casbindemo
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=casbindemo
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
